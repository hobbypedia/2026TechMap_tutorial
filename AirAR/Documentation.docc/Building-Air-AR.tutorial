@Tutorial(time: 90) {
    @Intro(title: "현재 위치의 공기와 햇빛을 AR로 표현하기") {
        SwiftUI, ARKit, RealityKit으로 숫자표가 아닌 움직이는 환경 시각화를 만듭니다. WeatherKit과 REST API라는 두 가지 요청 방식을 함께 사용해 각 데이터 소스의 장점을 결합합니다.

        @Image(source: "air-ar-hero.svg", alt: "현재 위치 환경 데이터 AR 앱의 완성 구조")
    }

    @Section(title: "1. 프로젝트와 권한 준비") {
        @ContentAndMedia {
            iOS 17 SwiftUI 앱과 Unit Test Target을 준비합니다. ARKit은 카메라와 모션 센서로 공간을 추적하므로 `NSCameraUsageDescription`이 필요합니다. 현재 지역의 데이터를 조회하려면 `NSLocationWhenInUseUsageDescription`도 필요합니다.

            @Image(source: "air-ar-hero.svg", alt: "SwiftUI와 ARKit으로 구성된 앱 구조")
        }
        @Steps {
            @Step {
                앱 Target과 테스트 Target의 배포 버전을 iOS 17로 맞추고 SwiftUI, ARKit, RealityKit을 연결합니다.
            }
            @Step {
                `Info.plist`에 카메라와 앱 사용 중 위치가 환경 데이터를 조회하고 공간에 표시하는 데 사용된다는 설명을 추가합니다.
            }
        }
    }

    @Section(title: "2. 화면용 환경 모델 만들기") {
        @ContentAndMedia {
            WeatherKit 프레임워크 타입과 REST 응답 DTO를 화면 전체에 퍼뜨리지 않습니다. 결합 서비스가 기온, PM2.5, UV Index, 풍속과 풍향을 하나의 `AirQualitySnapshot`으로 조합하면 SwiftUI와 RealityKit이 같은 값 타입을 공유할 수 있습니다.

            @Image(source: "world-coordinate-diagram.svg", alt: "환경 데이터가 AR 월드로 전달되는 흐름")
        }
        @Steps {
            @Step {
                위치 이름과 측정 시각을 포함한 도메인 모델을 작성합니다.

                @Code(name: "AirQualitySnapshot.swift", file: "01-AirQualitySnapshot.swift")
            }
        }
    }

    @Section(title: "3. WeatherKit으로 현재 기상 요청하기") {
        @ContentAndMedia {
            현재 좌표를 `CLLocation`으로 만들고 `WeatherService`에 `.current` 데이터만 요청합니다. Measurement 값을 섭씨, m/s, 도 단위로 변환하고 필수 Apple Weather 출처 정보도 함께 가져옵니다.

            @Image(source: "air-ar-hero.svg", alt: "두 API의 환경 데이터가 하나의 앱 모델로 합쳐지는 개념도")
        }
        @Steps {
            @Step {
                App ID와 Target에 WeatherKit capability를 활성화합니다.
            }
            @Step {
                WeatherKit 호출과 취소 오류를 앱 서비스 경계에서 변환합니다.

                @Code(name: "WeatherKitEnvironmentService.swift", file: "02-WeatherKitEnvironmentService.swift")
            }
        }
    }

    @Section(title: "4. REST API로 PM2.5 요청하고 결합하기") {
        @ContentAndMedia {
            WeatherKit에 없는 PM2.5만 Open-Meteo Air Quality API의 `current=pm2_5`로 요청합니다. URL과 JSON 디코딩은 이 제공자 안에 가두고, 기상 데이터와 중복되는 UV는 요청하지 않습니다.

            @Image(source: "air-ar-hero.svg", alt: "WeatherKit 기상 데이터와 REST 미세먼지 데이터가 합쳐지는 흐름")
        }
        @Steps {
            @Step {
                `URLComponents`와 `URLQueryItem`으로 현재 좌표와 PM2.5 변수만 포함한 HTTPS 주소를 만듭니다.

                @Code(name: "OpenMeteoPM25Provider.swift", file: "02-OpenMeteoPM25Provider.swift")
            }
            @Step {
                WeatherKit과 PM2.5 요청을 `async let`으로 동시에 실행하고 하나의 스냅샷으로 합칩니다.

                @Code(name: "CombinedEnvironmentService.swift", file: "02-CombinedEnvironmentService.swift")
            }
            @Step {
                Apple Weather, Open-Meteo와 Copernicus CAMS 출처를 화면에서 확인할 수 있게 전달합니다.
            }
        }
    }

    @Section(title: "5. ViewModel에서 최신 요청만 반영하기") {
        @ContentAndMedia {
            `@MainActor` ViewModel이 로딩 상태와 최신 스냅샷을 소유합니다. 새로고침이 들어오면 이전 Task를 취소하고 요청 세대 번호를 비교해 늦게 도착한 이전 결과가 화면을 덮어쓰지 못하게 합니다.

            @Image(source: "air-ar-hero.svg", alt: "서비스 결과를 SwiftUI 상태로 전달하는 흐름")
        }
        @Steps {
            @Step {
                먼저 공개 상태와 Task 저장 공간을 만듭니다.

                @Code(name: "AirQualityViewModel.swift", file: "03-AirQualityViewModel-start.swift")
            }
            @Step {
                로드, 새로고침, 취소와 최신 요청 판별을 완성합니다.

                @Code(name: "AirQualityViewModel.swift", file: "03-AirQualityViewModel.swift", previousFile: "03-AirQualityViewModel-start.swift")
            }
        }
    }

    @Section(title: "6. AR 월드 추적 시작하기") {
        @ContentAndMedia {
            이 앱은 바닥 평면 위에 놓는 앱이 아니라 공기 중에 환경 오브젝트를 띄웁니다. 따라서 평면 감지를 기다리지 않고 `ARWorldTrackingConfiguration`의 위치·방향 추적을 사용하며 Coaching Overlay로 초기 움직임을 안내합니다.

            @Image(source: "world-coordinate-diagram.svg", alt: "기기 카메라와 월드 좌표계의 관계")
        }
        @Steps {
            @Step {
                중력과 북쪽 기준의 `gravityAndHeading` 월드 정렬로 세션을 시작합니다.

                @Code(name: "ARSession.swift", file: "04-ARSession.swift")
            }
            @Step {
                `.normal`, `.limited`, 중단, 재개, 미지원 상태를 SwiftUI가 읽을 수 있는 값으로 변환합니다.
            }
        }
    }

    @Section(title: "7. 사용자 중심의 월드 공간 만들기") {
        @ContentAndMedia {
            북쪽 정렬 월드에서 회전 없는 변환에 최초 카메라 위치만 적용해 `AnchorEntity(world:)`를 생성합니다. 앵커의 X축은 동쪽, -Z축은 북쪽과 계속 일치하므로 기상 풍향을 올바른 AR 이동 벡터로 바꿀 수 있습니다.

            @Image(source: "air-ar-hero.svg", alt: "최초 사용자 위치를 중심으로 고정된 월드 공간 시각화")
        }
        @Steps {
            @Step {
                북쪽 기준 월드 축은 유지하고 카메라의 위치만 복사해 앵커를 고정합니다.

                @Code(name: "WorldCoordinate.swift", file: "05-WorldCoordinate.swift")
            }
        }
    }

    @Section(title: "8. PM2.5를 입자로 표현하기") {
        @ContentAndMedia {
            Open-Meteo PM2.5 농도를 투명한 황토색 먼지 스프라이트 개수로 바꿉니다. 풍향은 WeatherKit에서 가져오며 바람이 불어오는 방향의 180도 반대를 먼지 진행 벡터로 사용합니다. 풍속이 강할수록 빠르게 이동시킵니다.

            @Image(source: "dust-particle.png", alt: "투명 배경의 황토색 미세먼지 입자 디자인")
        }
        @Steps {
            @Step {
                `DustParticle.imageset`을 불러와 투명도가 낮은 `UnlitMaterial`에 연결합니다.
            }
            @Step {
                PM2.5 값으로 입자 수를 정하고 사용자 주변 360° 위치에 스프라이트를 생성합니다.

                @Code(name: "PM25Visualization.swift", file: "06-PM25Visualization.swift")
            }
            @Step {
                풍향과 풍속을 북쪽 기준 이동 벡터와 제한된 AR 속도로 변환하고 입자를 원형 영역 안에서 순환시킵니다.
            }
        }
    }

    @Section(title: "9. UV를 빛나는 공간 오브젝트로 표현하기") {
        @ContentAndMedia {
            UV Index는 게이지나 원형 문양 대신 실제 백색광이 빨강·주황·노랑·초록·파랑·보라 파장으로 퍼지는 스펙트럼으로 표현합니다. 광원 그룹은 최초 사용자 위치의 위쪽 한 월드 지점에 고정하고, 같은 광원점에서 45° 간격으로 방사되는 여덟 개의 경사진 투명 평면을 사용해 회전 방향에 따른 가시성 차이를 줄입니다. 크기는 약 1.65...2.0m로 화면의 약 3분의 2를 비추며, UV가 강할수록 낮게 시작한 불투명도가 올라갑니다.

            @Image(source: "uv-spectrum.png", alt: "투명 배경에서 백색광이 여러 색의 스펙트럼으로 퍼지는 자외선 디자인")
        }
        @Steps {
            @Step {
                `UVSpectrum.imageset` 텍스처를 사용자 위 1.45m의 같은 광원점에서 45° 간격으로 내려오는 여덟 평면 메시와 투명 재질에 적용합니다.
            }
            @Step {
                UV Index로 크기와 광량을 계산하고 광원 그룹을 사용자 바로 위 월드 좌표에 둡니다.

                @Code(name: "UVVisualization.swift", file: "07-UVVisualization.swift")
            }
        }
    }

    @Section(title: "10. 시각화를 자동 배치하고 움직이기") {
        @ContentAndMedia {
            데이터와 정상 추적 상태가 모두 준비되면 버튼 없이 월드 공간에 콘텐츠를 자동 배치합니다. `SceneEvents.Update`를 구독해 먼지는 현재 바람으로 이동시키면서 항상 카메라를 향하게 하고, UV 평면들은 함께 미세하게 맥동시킵니다. 새 데이터를 받으면 풍향과 풍속도 함께 갱신합니다.

            @Image(source: "air-ar-hero.svg", alt: "공간에 자동 배치된 환경 오브젝트")
        }
        @Steps {
            @Step {
                ARView Coordinator에 배치, 업데이트, 프레임 애니메이션을 연결합니다.

                @Code(name: "AirQualityARView.swift", file: "08-AirQualityARView.swift")
            }
        }
    }

    @Section(title: "11. 상단 정보와 새로고침만 남기기") {
        @ContentAndMedia {
            실제 앱의 조작을 최소화합니다. 카메라 위 상단에는 `온도 | 미세먼지 | UV`를 한 줄로 표시하고, 하단에는 새로고침 버튼 하나만 둡니다. 재배치와 초기화 버튼은 제거하고 AR 콘텐츠는 상태 변화에 맞춰 자동 관리합니다.

            @Image(source: "air-ar-hero.svg", alt: "상단 정보와 하단 새로고침으로 단순화한 앱 화면")
        }
        @Steps {
            @Step {
                SwiftUI 화면을 ARView, 컴팩트 정보 패널, 새로고침 버튼으로 구성합니다.

                @Code(name: "ContentView.swift", file: "09-ContentView.swift")
            }
        }
    }

    @Section(title: "12. 실제 기기에서 확인하기") {
        @ContentAndMedia {
            시뮬레이터는 실제 카메라와 모션 센서를 이용한 월드 트래킹을 검증할 수 없습니다. 컴파일과 단위 테스트 뒤 ARKit 지원 iPhone 또는 iPad에서 권한, 추적 안정화, 월드 고정 상태와 입자 애니메이션을 확인합니다.

            @Image(source: "world-coordinate-diagram.svg", alt: "실제 기기에서 360도 월드 공간 효과를 확인하는 방법")
        }
        @Steps {
            @Step {
                Signing Team을 선택하고 iOS 17 이상 실제 기기에서 앱을 실행합니다.
            }
            @Step {
                카메라 권한을 허용하고 주변을 천천히 비춥니다. 추적이 정상화되면 시각화가 자동으로 나타나는지 확인합니다.
            }
            @Step {
                제자리에서 기기를 360° 회전해 방향마다 비슷한 미세먼지 밀도가 보이는지 확인합니다. UV는 화면에 붙어 돌지 않고 최초 사용자 위치 위의 같은 월드 지점에 남는지 확인합니다.
            }
            @Step {
                새로고침 뒤 세 수치와 먼지·UV가 함께 갱신되고 Apple Weather, Open-Meteo와 CAMS 출처 링크가 표시되는지 확인합니다.
            }
        }
    }

    @Assessments {
        @MultipleChoice {
            PM2.5 값이 높아졌음을 표가 아닌 공간 디자인으로 전달하는 구현은 무엇인가요?
            @Choice(isCorrect: true) {
                낮은 불투명도의 황토색 먼지 스프라이트 개수를 늘리고 서로 다른 위상으로 부유시킨다.

                @Justification(reaction: "정답입니다!") {
                    데이터가 입자의 밀도와 움직임이라는 시각적 분위기로 변환됩니다.
                }
            }
            @Choice(isCorrect: false) {
                PM2.5 값을 표의 셀에만 출력한다.

                @Justification(reaction: "다시 생각해 보세요.") {
                    이 앱의 핵심은 수치를 공간의 밀도로 느끼게 하는 것입니다.
                }
            }
            @Choice(isCorrect: false) {
                값과 관계없이 항상 같은 먼지 이미지를 한 장 표시한다.

                @Justification(reaction: "다시 생각해 보세요.") {
                    입자 양이 데이터와 함께 변해야 합니다.
                }
            }
        }
    }
}
